<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tarea02">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TiendaVirtual2" android:label="@string/titulo_pantalla_datos_envio"></activity>
        <activity
            android:name=".TiendaVirtual1"
            android:label="@string/titulo_pantalla_hacer_pedido" />
        <activity
            android:name=".ComprasRealizadas"
            android:label="@string/titulo_pantalla_compras_realizadas" />
        <activity
            android:name=".PedidosEnTramite"
            android:label="@string/titulo_pantalla_pedidos_en_tramite" />
        <activity
            android:name=".HomeCliente"
            android:label="@string/titulo_pantalla_home_cliente" />
        <activity
            android:name=".LoginCliente"
            android:label="@string/titulo_pantalla_inicio" />
        <activity
            android:name=".HomeAdmin"
            android:label="@string/titulo_pantalla_home_administrador" />
        <activity
            android:name=".ErrorLogin"
            android:label="@string/titulo_pantalla_inicio" />
        <activity
            android:name=".LoginAdmin"
            android:label="@string/titulo_pantalla_inicio" />
        <activity
            android:name=".MainActivity"
            android:label="@string/titulo_pantalla_inicio">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>